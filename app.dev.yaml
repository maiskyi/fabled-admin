runtime: nodejs
env: flex
service: ks-dev
instance_class: F2

runtime_config:
  operating_system: ubuntu22

beta_settings:
  cloud_sql_instances: fabled-976c8:us-east1:development

automatic_scaling:
  min_num_instances: 1
  max_num_instances: 2

env_variables:
  SERVER_CORS_ORIGINS: "https://localhost:5173,https://app.dev.fabled.space,capacitor://localhost"
